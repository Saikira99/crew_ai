services:
  - type: web
    name: skillcapital-chatbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python skillcapital_chatbot_api.py
    envVars:
      - key: OPENAI_API_KEY
        sync: false